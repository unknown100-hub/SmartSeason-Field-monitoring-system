{
  "name": "smartseason-field-monitoring-system",
  "proxy": "http://localhost:5000",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend/latest run dev",
    "build": "npm --prefix frontend/latest run build",
    "lint": "npm --prefix frontend/latest run lint",
    "preview": "npm --prefix frontend/latest run preview",
    "backend": "npm --prefix backend start"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "react-router-dom": "^7.14.2",
    "react-scripts": "^5.0.1"
  }
}
